--

name: FkGitCommitInfoStd

on: [push, pull_request]

jobs:
  check:
    name: Check Commit Info
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git log

